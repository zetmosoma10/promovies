import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link to="/" className="text-lg md:text-2xl font-bold text-mintGreen">
      PROMOVIES
    </Link>
  );
};

export default Logo;
